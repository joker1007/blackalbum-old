- if (typeof p != "undefined")
  - if (req.q)
    != p.render({url: '/books/search/%N?q='+req.q, page: page})
  - else
    != p.render({url: '/books/%N', page: page})

!= partial('book', {object: books})

- if (typeof p != "undefined")
  - if (req.q)
    != p.render({url: '/books/search/%N?q='+req.q, page: page})
  - else
    != p.render({url: '/books/%N', page: page})

- if (count > 10)
  :coffeescript
    $("img.thumb").lazyload {
      placeholder : "/images/gray.gif"
      effect : "fadeIn"
    }
